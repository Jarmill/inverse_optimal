load('test3_data.mat');

n = length(y);
m = length(phi);

alpha0 = ones(m, 1)/m;

[P_func,P_selector] = cons_local_optimizers(y, Q, phi, Asdp, bsdp, Aeqsdp, beqsdp);


function f_out = f_in(alpha, P_func)

x

end